# 🚀 Comment Lancer le Projet

## Méthode Simple - 2 Terminaux

### Terminal 1 - Backend Django
```bash
./start_django.sh
```

### Terminal 2 - Frontend React
```bash
./start_frontend.sh
```

---

## URLs d'accès

- **Frontend** : http://192.168.1.55:8080
- **Backend API** : http://192.168.1.55:8000/api/
- **Django Admin** : http://192.168.1.55:8000/admin/

---

## Comptes de Test

| Rôle | Username | Password |
|------|----------|----------|
| Étudiant | etudiant1 | etud123 |
| Admin | admin | admin123 |
| Responsable | responsable | resp123 |
| Entreprise | datasolutions | entr123 |

---

## Scripts Disponibles

### Démarrage
- `./start_django.sh` - Lance uniquement Django
- `./start_frontend.sh` - Lance uniquement le frontend
- `./start.sh` - Lance les deux ensemble (en arrière-plan)

### Arrêt
- `./restart.sh` - Arrête tous les processus
- `CTRL+C` - Dans chaque terminal

---

## Ordre de Lancement

1. **D'abord Django** (Terminal 1)
   ```bash
   ./start_django.sh
   ```
   Attends de voir : "Starting development server..."

2. **Puis le Frontend** (Terminal 2)
   ```bash
   ./start_frontend.sh
   ```
   Attends de voir : "Local: http://..."

3. **Ouvre ton navigateur**
   ```
   http://192.168.1.55:8080
   ```

---

## Dépannage

### Port déjà utilisé
```bash
# Tuer Django
pkill -f "manage.py runserver"

# Tuer Vite
pkill -f "vite"

# Relancer
./start_django.sh    # Terminal 1
./start_frontend.sh  # Terminal 2
```

### Problème CORS
Les URLs sont configurées pour :
- Frontend : `http://192.168.1.55:8080`
- Backend : `http://192.168.1.55:8000`

Si ton IP change, modifie dans :
- `config/settings.py` (ALLOWED_HOSTS, CORS_ALLOWED_ORIGINS)
- `src/lib/api.ts` (API_BASE_URL)

---

## Vérifier que tout fonctionne

```bash
# Django répond
curl http://192.168.1.55:8000/api/offers/

# Frontend accessible
curl http://192.168.1.55:8080
```

---

**Pour plus de détails, voir :**
- `QUICKSTART.md` - Guide de démarrage rapide
- `TROUBLESHOOTING.md` - Guide de dépannage
- `RENDU_FINAL.md` - Document de rendu complet
